import { createNativeStackNavigator } from "@react-navigation/native-stack";
import {
    ScrollView,
    Text,
    StyleSheet,
    View,
    Image,
    SafeAreaView,
    TouchableOpacity
} from "react-native";

const stack = createNativeStackNavigator

const WaitingPaymentGateway = () => {
    return (

    )
};

export default WaitingPaymentGateway;